# Results

This directory contains only publication-safe, aggregate results. It must not contain patient-level predictions, training logs, checkpoints, or CT-derived images.

`tables/main_results.csv` reproduces the aggregate five-fold internal comparison reported in the submitted thesis. The `figures/` directory is intentionally empty until candidate figures have passed a separate data-governance review.
