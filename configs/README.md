# Configuration

The notebooks retain the original research implementation and expect authorised local data paths. Configure data and output locations in each notebook's initial configuration cell. Do not commit local paths, credentials, or clinical-data manifests here.
